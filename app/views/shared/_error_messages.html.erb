<% if object.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      エラーが <%= pluralize(object.errors.count, "個") %>.
    </div>
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>